# 用例书写规范

适用范围：高途矩阵 App 自动化用例编写，供用例设计者参考。

---

## 预置条件：起始页描述

自动化执行前会根据预置条件定位起始页面，**起始页描述必须使用以下标准词**，否则导航可能定位到错误页面。

| 起始页 | 标准写法（任选其一） |
|--------|-------------------|
| 上课 tab | 当前在上课 tab、当前在上课页 |
| 课程详情页 | 当前在课程详情页 |
| 课节列表页 | 当前在课节列表页 |
| 课节详情页 | 当前在课节详情页 |
| 搜索页 | 当前在搜索页 |
| AI闪学 tab | 当前在 AI闪学 tab、当前在闪学 tab |
| 首页 tab | 当前在首页、当前在首页 tab |
| 我的页 | 当前在我的页、当前在我的 |
| 设置页 | 当前在设置页 |
| 消息 tab | 当前在消息 tab |
| 登录页 | 当前在登录页、未登录状态 |

> 写 "进入上课 tab" / "打开首页" 等**动作描述**不会被识别为起始页，请统一用"当前在 XX"格式。

---

## 单条用例写法

```
预置条件：
  当前在课程详情页
  已登录账号 12100000000

步骤：
  1. 点击某课节
  2. 预期：进入课节详情页，显示课节标题
```

---

## 链路用例写法

多条用例连续执行时，**每条用例的预置条件描述上一条结束后所在的页面**，自动化会跳过重复导航直接继续。

### 示例：课程购买 → 进入教室 链路

**用例 1：课程购买**
```
预置条件：
  当前在课程详情页

步骤：
  1. 点击立即购买
  2. 完成支付
  3. 预期：购买成功，返回课程详情页
```

**用例 2：进入教室**
```
预置条件：
  当前在课节列表页        ← 承接用例 1 的结束状态

步骤：
  1. 点击目标课节
  2. 点击进教室
  3. 预期：成功进入直播教室
```

**用例 3：退出教室**
```
预置条件：
  当前在课节详情页        ← 承接用例 2 的结束状态（退出教室后回到此页）

步骤：
  1. 点击退出
  2. 确认退出
  3. 预期：返回课节详情页
```

---

---

## 预置条件详细说明

预置条件描述"从哪开始、用哪个账号、数据状态是什么"，自动化在正式步骤前逐条处理，失败则中止用例。

### 支持的写法与执行动作

| 预置条件 | 执行动作 |
|---------|---------|
| 起始页（标准词，见上表） | 导航到目标页面 |
| 已登录账号 12100000000 | 检查当前登录账号，不符则重新登录 |
| 无需登录 / 未登录状态 | 跳过所有登录操作 |
| 重启App：是 | 执行 App 重启 |
| 重启App：否 / 不填 | 只确认 App 在前台，不重启 |
| 账号有已购课程（数据条件） | 截图确认，记为前置检查，不触发操作 |

> 预置条件截图只在**失败时**保存，正常通过不截图。

---

## 测试步骤详细说明

每行一个操作，用序号分隔，口语化描述即可。自动化按文字意图映射操作：

| 步骤描述特征 | 执行方式 |
|---|---|
| 点击 / 进入 / 选择 | AI 视觉定位 → tap |
| 输入 / 填写 | 定位输入框 → set_value |
| 滑动 / 滚动 | gesture scroll/swipe |
| 等待 / 加载 | 轮询等条件出现，不用固定 sleep |

> 步骤执行后若下一步是验证，不截图，交给验证步骤统一截图。

---

## 预期结果与验证点的区别

两者都是验证步骤（ASSERT），区别在粒度：

| 字段 | 定位 | 写法建议 |
|------|------|---------|
| **验证点** | 具体可断言的观测项，每条独立 | "列表显示 3 条记录"、"按钮变为灰色不可点击" |
| **预期结果** | 整体业务结论，用例是否通过的最终定性 | "功能操作成功，数据正确展示" |

执行顺序：先执行验证点（逐条截图核对），再执行预期结果（整体判定）。验证点可单独标记失败，预期结果是最终 pass/fail 依据。

> 建议两个字段都填：验证点写细节，预期结果写结论。只填一个时，自动化会将有值的字段作为唯一 ASSERT 步骤。

---

## 常见错误写法

| 错误写法 | 问题 | 正确写法 |
|---------|------|---------|
| 进入上课 tab | 动作描述，无法识别为起始页 | 当前在上课 tab |
| 已在课程列表 | "课程列表"不在标准词表 | 当前在课程详情页 |
| 打开 App 首页 | 动作描述 | 当前在首页 tab |
| 无预置条件 | 自动化会默认导航到上课 tab | 明确写出起始页 |
| 链路用例预置条件与上一条结束页不符 | 触发不必要导航，打断链路 | 预置条件与上一条结束状态保持一致 |
| 验证点只写"正常显示" | 无法判断通过条件 | 写具体可观测的状态，如"显示XX文字/按钮" |
